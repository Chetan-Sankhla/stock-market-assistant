# Core dependencies
pandas>=2.2.0
numpy>=1.25.0
streamlit>=1.32.0
yfinance>=0.2.30
matplotlib>=3.8.0
plotly>=5.20.0
scikit-learn>=1.4.0
joblib>=1.3.0
pytz>=2023.3
datetime  # Standard library (doesn't need to be included)

# Signal and statistics
scipy>=1.12.0

# TA indicators and pattern detection
pandas-ta>=0.3.14b0

# For TA-Lib (compiled C library for technical indicators)
# Use binary wheel for Windows. Native compile for Linux/Mac.
TA-Lib==0.6.3; platform_system != "Windows" or platform_machine != "AMD64"
TA-Lib-binary; platform_system == "Windows" and platform_machine == "AMD64"
